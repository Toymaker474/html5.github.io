import assert from 'node:assert/strict';
import { createWorld, CHUNK_COUNT, DISTRICT_TYPES, BIOMES, chunkIndexFor, rebuildOccupancy, takeChunkBatch, updateEnvironment } from '../src/world.js';
import { createBrain, chooseAction, ACTIONS } from '../src/brain.js';
import { emptyLibrary } from '../src/evolution.js';
const i=(op,arg=0)=>({op,arg}),w=createWorld(123);assert.equal(w.chunks.length,CHUNK_COUNT);assert.equal(chunkIndexFor(0,0),0);assert.equal(chunkIndexFor(1199,799),CHUNK_COUNT-1);const biomes=new Set(w.chunks.map(c=>c.biome));assert.ok(biomes.size>=4);assert.ok(biomes.has('RIVER'));for(const b of biomes)assert.ok(BIOMES.includes(b));const sites=new Set(w.chunks.map(c=>c.site));for(const s of sites)assert.ok(DISTRICT_TYPES.includes(s));for(const needed of['HOME','FARM','WORK','LAB'])assert.ok(sites.has(needed));const farm=w.chunks.find(c=>c.site==='FARM'),home=w.chunks.find(c=>c.site==='HOME'),lab=w.chunks.find(c=>c.site==='LAB'),work=w.chunks.find(c=>c.site==='WORK');assert.ok(farm.food>.65);assert.ok(home.shelter>.65);assert.ok(lab.jobs>.60&&lab.learning===1);assert.ok(w.chunks.some(c=>c.biome==='FOREST'&&c.wood>.60));assert.ok(w.chunks.some(c=>c.biome==='RIVER'&&c.water===1));const beforeFood=farm.food;updateEnvironment(farm,{floodLevel:0,rain:0});assert.ok(farm.food>=beforeFood);updateEnvironment(farm,{floodLevel:1,rain:1});assert.ok(farm.flood>0);const library=emptyLibrary();library.IS_AHEAD={program:[i('PUSH_A'),i('PUSH_B'),i('GT'),i('HALT')],report:{verified:true}};library.PICK_LOW={program:[i('PUSH_A'),i('PUSH_B'),i('LT'),i('JZ',2),i('PUSH_A'),i('HALT'),i('PUSH_B'),i('HALT')],report:{verified:true}};work.drainage=0;updateEnvironment(work,{floodLevel:1,rain:1});assert.equal(work.floodAlarm,true);assert.equal(work.pumpActive,true);assert.ok(work.pumpRate>0);const agents=[{alive:true,x:10,y:10},{alive:true,x:1100,y:700},{alive:false,x:5,y:5}];rebuildOccupancy(w,agents);assert.equal(w.chunks.reduce((n,c)=>n+c.agents.length,0),2);assert.equal(takeChunkBatch(w,8).length,8);let seed=.314159;const rng=()=>{seed=(seed*9301+49297)%233280;return seed/233280;},brain=createBrain(rng);assert.equal(brain.length,63);assert.ok(ACTIONS.includes(chooseAction(brain,{hunger:1,food:1},rng)));console.log(`PASS: ${CHUNK_COUNT} chunks, ${biomes.size} biomes, functional sites, flooding, evolved alarms/pumps, scheduler and survival brain.`);
