){m.ichor+=4+Math.random()*9;m.cooldown=Math.max(0,m.cooldown-.08);incoming*=.35;immediate+=2+g.curiosity*2;}
  else if(m.action==='Extract'){m.state='extracted';m.fitness=scoreMind(m);immediate+=20;remember(m,`The portal accepted the build with ${m.ichor|0} Ichor and ${m.loot.toFixed(1)} loot value.`);if(m.id===selected){sound.portal();showEnd(m)}updateBandit(m,immediate);return;}
  if(Math.random()<incoming){m.hp--;m.hurt=.5;m.streak=0;immediate-=12;remember(m,`Impact. ${m.hp} Heart${m.hp===1?'':'s'} remain.`);if(m.id===selected){sound.damage();flashDamage();spawnBurst('blood',22)}}
  if(m.hp<=0){const revive=(H.revive||0)+(F.revive||0);if(!m.revived&&Math.random()<revive*.35){m.revived=true;m.hp=Math.max(1,Math.ceil(m.maxHp*.65));immediate+=8;remember(m,'A revival effect interrupted death. Re-plan immediately.');if(m.id===selected){sound.revive();spawnBurst('ichor',40)}}else{m.state='dead';m.fitness=scoreMind(m);immediate-=18;remember(m,'The body failed. Current VOIN no longer drops Ichor on death, but this trial ends here.');if(m.id===selected){sound.death();showEnd(m)}updateBandit(m,immediate);return}}
  if(m.foes<=0){m.wave++;if(m.wave>9){m.state='extracted';m.fitness=scoreMind(m);remember(m,'All modeled encounters cleared. The lineage returns with evidence.');if(m.id===selected){sound.portal();showEnd(m)}}else{m.foes=4+Math.floor(m.wave*1.3)+Math.floor(Math.random()*4);m.chest=Math.random()<.62;remember(m,`Encounter ${m.wave}: ${m.foes} new otvrats move through the ruins.`);if(m.id===selected)syncEnemyCount()}}
  const after=m.ichor*.018+m.loot*4+m.kills*1.5+m.hp*8;updateBandit(m,immediate+(after-m.lastRewardBase));
}
function tickMind(m,dt){if(m.state!=='active')return;m.time+=dt;m.nextDecision-=dt;m.swing=Math.max(0,m.swing-dt);m.hurt=Math.max(0,m.hurt-dt);m.dash=Math.max(0,m.dash-dt);m.lightning=Math.max(0,m.lightning-dt);m.parryWindow=Math.max(0,m.parryWindow-dt);m.cooldown=Math.max(0,m.cooldown-dt);m.powerCharge=clamp(m.powerCharge+dt*.04);if(m.nextDecision<=0){chooseAction(m);resolveAction(m);m.objectives=objectives(m);m.fitness=scoreMind(m);const s=combatStats(m);m.nextDecision=(.22+Math.random()*.25)/(m.action==='Attack'?Math.max(.7,s.speed):1);totalEvaluations++}}
function objectives(m){const mins=Math.max(.25,m.time/15);const survived=m.state==='extracted'?1:m.state==='active'?.35:0;const style={aggression:m.g.aggression,greed:m.g.greed,skillspam:1-combatStats(m).cooldown,mobility:combatStats(m).mobility};return {survival:survived,ichor:m.ichor/mins,loot:m.loot/mins,dps:m.kills/mins*45,learning:m.learnedActions,style}}
function scoreMind(m){const o=m.objectives||objectives(m),goal=$('goal').value;if(goal==='survival')return o.survival*700+o.dps*.15;if(goal==='ichor')return o.ichor+o.survival*180;if(goal==='loot')return o.loot*100+o.survival*180;if(goal==='speed')return o.dps*1.2+o.survival*140;return o.survival*230+o.ichor*.45+o.loot*65+o.dps*.24+m.novelty*80}
function behaviorDistance(a,b){const x=(a.objectives||objectives(a)).style,y=(b.objectives||objectives(b)).style;return Math.hypot(x.aggression-y.aggression,x.greed-y.greed,x.skillspam-y.skillspam,x.mobility-y.mobility)}
function assignNovelty(pop){for(const p of pop){const d=pop.filter(q=>q!==p).map(q=>behaviorDistance(p,q)).sort((a,b)=>a-b).slice(0,7);p.novelty=avg(d)}}
function dominates(a,b){const A=a.objectives||objectives(a),B=b.objectives||objectives(b);const av=[A.survival,A.ichor,A.loot,A.dps,a.novelty],bv=[B.survival,B.ichor,B.loot,B.dps,b.novelty];return av.every((v,i)=>v>=bv[i])&&av.some((v,i)=>v>bv[i])}
function nondominated(pop){return pop.filter(p=>!pop.some(q=>q!==p&&dominates(q,p)))}
function nicheKey(m){const o=m.objectives||objectives(m);return `${Math.min(4,Math.floor(o.style.aggression*5))}:${Math.min(4,Math.floor(o.style.greed*5))}:${Math.min(4,Math.floor(o.style.skillspam*5))}`}
function updateArchive(pop){for(const m of pop){const k=nicheKey(m),old=archive.get(k);if(!old||scoreMind(m)>scoreMind(old))archive.set(k,{g:cloneGenome(m.g),score:scoreMind(m),objectives:m.objectives})}}
function cross(a,b,i){const g={name:NAMES[i],line:Math.random()<.5?a.line:b.line};for(const s of SLOTS)g[s]=Math.random()<.5?a[s]:b[s];for(const t of TRAITS)g[t]=clamp((a[t]+b[t])/2+gauss()*.055);const rate=+$('mut').value/100;for(const s of SLOTS)if(Math.random()<rate)g[s]=rand(GEAR_KEYS[s]);for(const t of TRAITS)if(Math.random()<rate*1.5)g[t]=clamp(g[t]+gauss()*.2);return g}
function breed(){assignNovelty(minds);minds.forEach(m=>{m.objectives=objectives(m);m.fitness=scoreMind(m)});updateArchive(minds);const front=nondominated(minds),ranked=[...minds].sort((a,b)=>b.fitness-a.fitness);const parents=[...front,...ranked].filter((v,i,a)=>a.indexOf(v)===i).slice(0,12);generation++;minds=NAMES.slice(0,POP_SIZE).map((_,i)=>spawnMind(i<3?cloneGenome(ranked[i].g):cross(rand(parents).g,rand(parents).g,i),i));selected=0;hideEnd();rebuildSelectedWorld(true);renderUI(true)}

// ----- Procedural audio -----
class SoundEngine{
  constructor(){this.ctx=null;this.master=null;this.enabled=false;this.ambientStarted=false}
  async enable(){if(!this.ctx){const AC=window.AudioContext||window.webkitAudioContext;this.ctx=new AC();this.master=this.ctx.createGain();this.master.gain.value=.35;this.master.connect(this.ctx.destination)}if(this.ctx.state==='suspended')await this.ctx.resume();this.enabled=true;this.master.gain.setTargetAtTime(.35,this.ctx.currentTime,.04);if(!this.ambientStarted)this.startAmbient()}
  disable(){this.enabled=false;if(this.master)this.master.gain.setTargetAtTime(0,this.ctx.currentTime,.05)}
  wake(){if(this.enabled&&this.master)this.master.gain.setTargetAtTime(.35,this.ctx.currentTime,.05)}
  tone(freq,dur=.12,type='sine',gain=.08,slide=0){if(!this.enabled||!this.ctx)return;const t=this.ctx.currentTime,o=this.ctx.createOscillator(),g=this.ctx.createGain(),f=this.ctx.createBiquadFilter();o.type=type;o.frequency.setValueAtTime(freq,t);if(slide)o.frequency.exponentialRampToValueAtTime(Math.max(20,freq+slide),t+dur);f.type='lowpass';f.frequency.value=4200;g.gain.setValueAtTime(gain,t);g.gain.exponentialRampToValueAtTime(.0001,t+dur);o.connect(f);f.connect(g);g.connect(this.master);o.start(t);o.stop(t+dur)}
  noise(dur=.15,gain=.08,filter=1200){if(!this.enabled||!this.ctx)return;const n=this.ctx.sampleRate*dur,b=this.ctx.createBuffer(1,n,this.ctx.sampleRate),d=b.getChannelData(0);for(let i=0;i<n;i++)d[i]=(Math.random()*2-1)*(1-i/n);const s=this.ctx.createBufferSource(),g=this.ctx.createGain(),f=this.ctx.createBiquadFilter();s.buffer=b;f.type='bandpass';f.frequency.value=filter;f.Q.value=.7;g.gain.value=gain;s.connect(f);f.connect(g);g.connect(this.master);s.start()}
  startAmbient(){if(!this.ctx||this.ambientStarted)return;this.ambientStarted=true;const t=this.ctx.currentTime;const drone=this.ctx.createOscillator(),drone2=this.ctx.createOscillator(),gain=this.ctx.createGain(),filter=this.ctx.createBiquadFilter();drone.type='sawtooth';drone2.type='sine';drone.frequency.value=43;drone2.frequency.value=