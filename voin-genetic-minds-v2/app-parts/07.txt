oal:$('goal').value,difficulty:+$('hard').value,population:144,generations:28,trials:14,mutation:+$('mut').value/100})}
function renderOptimizerResults(results){$('optimizerResults').innerHTML=results.slice(0,12).map((r,i)=>{const b=r.build,m=r.metrics;return `<article class="result"><div class="rank">${i+1}</div><div><h3>${b.weapon} + ${b.heart}</h3><p>${b.artifact} · ${b.rune} · ${b.ring}<br>${b.necklace} · ${b.soulfruit}${m.community?.name&&m.community.name!=='None'?`<br>Closest community prior: ${m.community.name}`:''}</p></div><div class="result-metrics"><span>${Math.round(m.survival*100)}% survive</span><span>${Math.round(m.ichor)} I/min</span><span>${m.loot.toFixed(1)} loot/min</span><span>${Math.round(m.dps)} DPS</span><span>${m.clearTime.toFixed(1)}s clear</span><span>${m.score.toFixed(0)} score</span></div></article>`}).join('')}

// ----- UI -----
function bar(name,value,hot=false){return `<div class="bar ${hot?'hot':''}"><span>${name}</span><i><b style="width:${clamp(value)*100}%"></b></i><em>${Math.round(clamp(value)*100)}%</em></div>`}
function renderEvidence(){$('evidence').innerHTML=COMMUNITY_SEEDS.map(s=>`<article class="evidence-card"><header><b>${s.name}</b><span class="confidence">${Math.round(s.confidence*100)}% prior</span></header><p>${s.note}</p></article>`).join('')}
function renderUI(force=false){const m=minds[selected];if(!m)return;assignNovelty(minds);minds.forEach(x=>{x.objectives=objectives(x);x.fitness=scoreMind(x)});const ranked=[...minds].sort((a,b)=>b.fitness-a.fitness);$('gen').textContent=generation;$('alive').textContent=minds.filter(x=>x.state==='active').length;$('best').textContent=ranked[0].fitness.toFixed(0);$('archive').textContent=archive.size;$('evaluations').textContent=totalEvaluations.toLocaleString();$('agentName').textContent=m.g.name.toUpperCase();$('lineage').textContent=m.g.line.toUpperCase();$('ichor').textContent=m.ichor|0;$('status').textContent=playing?'MINDS ACTIVE':'PAUSED';$('action').textContent=m.action.toUpperCase();$('thought').textContent=m.thought;$('hearts').innerHTML=Array.from({length:m.maxHp},(_,i)=>`<i class="heart ${i<m.hp?'':'empty'}"></i>`).join('');
  $('buildTitle').textContent=`${m.g.weapon} · ${m.g.heart}`;$('build').innerHTML=[['Heart',m.g.heart],['Weapon',m.g.weapon],['Artifact',m.g.artifact],['Rune',m.g.rune],['Ring',m.g.ring],['Necklace',m.g.necklace],['Soulfruit',m.g.soulfruit],['Synergy',`${Math.round(synergy(m.g)*100)}%`]].map(([a,b])=>`<div class="build-item"><small>${a}</small><b>${b}</b></div>`).join('');
  $('intention').textContent=m.intention;$('beliefs').innerHTML=[['Threat',m.beliefs.threat],['Profit',m.beliefs.profit],['Escape',m.beliefs.escape],['Unknown',m.beliefs.uncertainty]].map(([a,b])=>`<div class="belief"><small>${a}</small><b>${Math.round(b*100)}%</b></div>`).join('');
  const ds=Object.entries(m.decisions),max=Math.max(.1,...ds.map(x=>x[1]));$('decisionWinner').textContent=ds.length?ds.sort((a,b)=>b[1]-a[1])[0][0]:'Waiting';$('decisions').innerHTML=ds.length?ds.slice(0,7).map(([n,v])=>bar(n,Math.max(0,v)/max,true)).join(''):'Waiting for perception…';$('traits').innerHTML=[['Aggression',m.g.aggression],['Greed',m.g.greed],['Fear',m.g.fear],['Curiosity',m.g.curiosity],['Discipline',m.g.discipline],['Reflex',m.g.reflex]].map(x=>bar(...x)).join('');
  $('populationSummary').textContent=`${minds.filter(x=>x.state==='dead').length} dead · ${minds.filter(x=>x.state==='extracted').length} extracted · alpha ${ranked[0].g.name}`;$('learningSummary').textContent=`${m.learnedActions} learned actions · ${Object.keys(m.bandit).length} state beliefs`;
  $('agents').innerHTML=minds.slice(0,15).map((x,i)=>`<button class="agent ${i===selected?'on':''}" data-i="${i}"><div class="agent-head"><b>${x.g.name}</b><span class="state">${x.state}</span></div><div class="mini-hearts">${'●'.repeat(Math.max(0,x.hp))}</div><small>${x.g.weapon}<br>${x.g.heart}<br>${x.ichor|0} Ichor · ${x.kills} kills · ${x.fitness|0} fit</small></button>`).join('');document.querySelectorAll('.agent').forEach(b=>b.onclick=()=>{selected=+b.dataset.i;rebuildSelectedWorld(false);if(minds[selected].state!=='active')showEnd(minds[selected]);renderUI(true)});$('memory').innerHTML=m.memory.length?m.memory.map(x=>`<div>[G${generation}] ${x}</div>`).join(''):'No memories yet.';
}
function simulationLoop(t){const dt=Math.min(.04,(t-lastSim)/1000);lastSim=t;if(playing){for(const m of minds)tickMind(m,dt);if(minds.every(m=>m.state!=='active')){playing=false;if(autoLeft>0){autoLeft--;setTimeout(()=>{breed();playing=true;$('run').textContent='PAUSE MINDS'},260)}}if(t-lastUI>100){renderUI();lastUI=t}}requestAnimationFrame(simulationLoop)}

$('run').onclick=async()=>{if(!sound.ctx&&$('sound').getAttribute('aria-pressed')==='true')await sound.enable();sound.wake();playing=!playing;$('run').textContent=playing?'PAUSE MINDS':'AWAKEN MINDS';renderUI(true)};
$('breed').onclick=()=>{playing=false;$('run').textContent='AWAKEN MINDS';breed()};$('auto').onclick=()=>{autoLeft=10;breed();playing=true;$('run').textContent='PAUSE MINDS'};$('optimizer').onclick=runOptimizer;$('reset').onclick=reset;
$('sound').onclick=async()=>{const on=$('sound').getAttribute('aria-pressed')!=='true';$('sound').setAttribute('aria-pressed',String(on));$('sound').textContent=on?'SOUND ON':'SOUND OFF';if(on){await sound.enable();sound.wake()}else sound.disable()};
$('hard').oninput=e=>$('hardOut').textContent=e.target.value;$('mut').oninput=e=>$('mutOut').textContent=`${e.target.value}%`;$('swarm').oninput=e=>{$('swarmOut').textContent=e.target.value;buildSwarm()};$('region').onchange=()=>{buildWorld();rebuildSelectedWorld(false)};$('goal').onchange=()=>renderUI(true);
$('modelFile').onchange=e=>{const f=e.target.files?.[0];if(!f)return;loadModelFrom(URL.createObjectURL(f),f.name)};$('loadModel').onclick=()=>{const u=$('modelUrl').value.trim();if(u)loadModelFrom(u,'Remote GLB')};

renderEvidence();reset();loadDefaultModel();requestAnimationFrame(simulationLoop);renderLoop();
