64.5;gain.gain.value=.015;filter.type='lowpass';filter.frequency.value=180;drone.connect(filter);drone2.connect(filter);filter.connect(gain);gain.connect(this.master);drone.start(t);drone2.start(t);const lfo=this.ctx.createOscillator(),lfg=this.ctx.createGain();lfo.frequency.value=.08;lfg.gain.value=.006;lfo.connect(lfg);lfg.connect(gain.gain);lfo.start(t)}
  attack(heavy=false){this.noise(heavy?.24:.14,heavy?.13:.08,heavy?520:1100);this.tone(heavy?95:160,heavy?.2:.1,'sawtooth',heavy?.08:.04,heavy?-55:-70)}
  hit(){this.noise(.12,.11,380);this.tone(58,.11,'square',.04,-18)} damage(){this.noise(.18,.12,220);this.tone(82,.22,'sawtooth',.08,-48)}
  dash(){this.noise(.22,.08,1800);this.tone(260,.16,'sine',.035,220)} parry(){this.tone(1500,.12,'triangle',.08,900);this.tone(720,.18,'sine',.04,-250)}
  lightning(){this.noise(.28,.12,2600);this.tone(90,.36,'sawtooth',.07,-45);this.tone(880,.2,'square',.025,-500)} pickup(){this.tone(520,.08,'sine',.035,300);setTimeout(()=>this.tone(760,.1,'sine',.03,220),55)}
  chest(bad){if(bad){this.noise(.35,.15,650);this.tone(70,.4,'sawtooth',.1,-35)}else{this.tone(310,.18,'triangle',.05,220);setTimeout(()=>this.tone(560,.25,'sine',.04,350),90)}}
  portal(){this.tone(110,.8,'sine',.05,420);this.tone(220,.65,'triangle',.03,620)} death(){this.noise(.6,.12,180);this.tone(90,.8,'sawtooth',.09,-60)} revive(){this.tone(120,.5,'sine',.05,620);setTimeout(()=>this.tone(480,.5,'triangle',.05,480),120)}
}
const sound=new SoundEngine();

// ----- Three.js world -----
const container=$('viewport');
const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.08;container.appendChild(renderer.domElement);
const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(72,16/9,.1,180);camera.position.set(0,1.66,2.3);scene.add(camera);
const composer=new EffectComposer(renderer),renderPass=new RenderPass(scene,camera),bloom=new UnrealBloomPass(new THREE.Vector2(1,1),.92,.72,.58),afterimage=new AfterimagePass(.84);afterimage.enabled=false;composer.addPass(renderPass);composer.addPass(bloom);composer.addPass(afterimage);composer.addPass(new OutputPass());
const world=new THREE.Group(),enemyRoot=new THREE.Group(),swarmRoot=new THREE.Group(),fxRoot=new THREE.Group();scene.add(world,swarmRoot,enemyRoot,fxRoot);
const hemi=new THREE.HemisphereLight(0x9cb7df,0x2a160c,1.0),sun=new THREE.DirectionalLight(0xffe5c1,2.1),blue=new THREE.PointLight(0x5ed8ff,4,18,2),fire=new THREE.PointLight(0xff6a32,6,20,2);sun.position.set(6,11,3);sun.castShadow=true;sun.shadow.mapSize.set(1536,1536);scene.add(hemi,sun,blue,fire);
const loader=new GLTFLoader(),clock=new THREE.Clock();let defaultAsset=null,customAsset=null,defaultAnimations=[],customAnimations=[],enemyVisuals=[],swarmMesh=null,weaponRig=null,portal=null,chest=null,sky=null,shockwaves=[],particles=[],lightningLines=[],visualLightning=0,lastFoes=-1;
function resize(){const r=container.getBoundingClientRect();if(!r.width||!r.height)return;renderer.setSize(r.width,r.height,false);composer.setSize(r.width,r.height);camera.aspect=r.width/r.height;camera.updateProjectionMatrix()}window.addEventListener('resize',resize);
function disposeObject(o){o.traverse?.(x=>{x.geometry?.dispose?.();if(Array.isArray(x.material))x.material.forEach(m=>m.dispose?.());else x.material?.dispose?.()})}
function clearGroup(g,dispose=true){while(g.children.length){const c=g.children[0];g.remove(c);if(dispose)disposeObject(c)}}
function clearEnemies(){while(enemyRoot.children.length){const c=enemyRoot.children[0];enemyRoot.remove(c);c.traverse?.(o=>{if(Array.isArray(o.material))o.material.forEach(m=>m.dispose?.());else o.material?.dispose?.()})}}
function terrainGeometry(){const geo=new THREE.PlaneGeometry(46,90,70,120),p=geo.attributes.position;for(let i=0;i<p.count;i++){const x=p.getX(i),y=p.getY(i);const h=Math.sin(x*.48)*.12+Math.sin(y*.22+x*.35)*.18+Math.sin(y*.65)*.05+(Math.random()-.5)*.08;p.setZ(i,h)}geo.computeVertexNormals();return geo}
function buildSky(top,bottom){if(sky){scene.remove(sky);disposeObject(sky)}const mat=new THREE.ShaderMaterial({side:THREE.BackSide,uniforms:{top:{value:new THREE.Color(top)},bottom:{value:new THREE.Color(bottom)}},vertexShader:`varying vec3 vP;void main(){vP=position;gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.);}`,fragmentShader:`uniform vec3 top;uniform vec3 bottom;varying vec3 vP;void main(){float h=normalize(vP).y*.5+.5;gl_FragColor=vec4(mix(bottom,top,smoothstep(.05,.95,h)),1.);}`});sky=new THREE.Mesh(new THREE.SphereGeometry(150,24,16),mat);scene.add(sky)}
function tower(x,z,h=8,r=2){const g=new THREE.Group(),mat=new THREE.MeshStandardMaterial({color:0x615747,roughness:.94});const body=new THREE.Mesh(new THREE.CylinderGeometry(r*.88,r,h,14),mat);body.position.y=h/2;body.castShadow=true;body.receiveShadow=true;g.add(body);for(let i=0;i<7;i++){const merlon=new THREE.Mesh(new THREE.BoxGeometry(.45,.7,.5),mat);const a=i/7*Math.PI*2;merlon.position.set(Math.cos(a)*r*.82,h+.2,Math.sin(a)*r*.82);merlon.rotation.y=-a;g.add(merlon)}g.position.set(x,0,z);world.add(g)}
function arch(x,z,scale=1){const g=new THREE.Group(),mat=new THREE.MeshStandardMaterial({color:0x4f4a43,roughness:.96});for(const sx of [-1,1]){const p=new THREE.Mesh(new THREE.BoxGeometry(.8*scale,5*scale,1.2*scale),mat);p.position.set(sx*1.65*scale,2.5*scale,0);p.castShadow=true;g.add(p)}const top=new THREE.Mesh(new THREE.BoxGeometry(4.1*scale,.8*scale,1.2*scale),mat);top.position.y=5*scale;g.add(top);g.position.set(x,0,z);world.add(g)}
function mountain(x,z,s,c){const m=new THREE.Mesh(new THREE.ConeGeometry(s,s*2.5,8,4),new THREE.MeshStandardMaterial({color:c,roughness:1}));m.position.set(x,s*1.1,z);m.rotation.y=Math.random()*Math.PI;m.receiveShadow=true;world.add(m)}
function buildWorld(){
  clearGroup(world);clearGroup(swarmRoot);const region=$('region').value;let ground=0x30291f,fog=0x88847e,top=0x969fb0,bottom=0xc49a74,accent=0x5ddcff;
  if(region==='cathedral'){ground=0x1a1715;fog=0x18191e;top=0x161822;bottom=0x5b4b43;accent=0x63d9ff}else if(region==='inferno'){ground=0x21110d;fog=0x38100a;top=0x160607;bottom=0xb13b16;accent=0xff5e31}else if(region==='frozen'){ground=0x1d2731;fog=0x9fb0bd;top=0x8ea5b9;bottom=0xd3d8db;accent=0x69cfff}
  scene.fog=new THREE.FogExp2(fog,region==='hollow'?.014:.022);buildSky(top,bottom);
  const floor=new THREE.Mesh(terrainGeometry(),new THREE.MeshStandardMaterial({color:ground,roughness:.97,metalness:.02}));floor.rotation.x=-Math.PI/2;floor.position.z=-35;floor.receiveShadow=true;world.add(floor);
  for(let i=0;i<180;i++){const s=new THREE.Mesh(new THREE.DodecahedronGeometry(.08+Math.random()*.42,0),new THREE.MeshStandardMaterial({color:region==='frozen'?0x586876:0x3d3932,roughness:1}));s.scale.y=.35;s.posi