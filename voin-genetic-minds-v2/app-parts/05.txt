tion.set((Math.random()-.5)*22,.04,-Math.random()*76);s.rotation.y=Math.random()*Math.PI;s.castShadow=true;world.add(s)}
  if(region==='hollow'){tower(-7,-22,10,2.5);tower(7,-25,12,2.8);tower(-9,-47,15,3);tower(9,-50,13,2.7);arch(0,-18,1.1);for(let i=0;i<12;i++)mountain((Math.random()-.5)*80,-70-Math.random()*40,8+Math.random()*16,0x4d5050)}
  else if(region==='cathedral'){for(let z=0;z<8;z++){arch(-5,-8-z*8,.8);arch(5,-11-z*8,.75)}tower(0,-55,20,4)}
  else if(region==='inferno'){for(let i=0;i<18;i++){const lava=new THREE.Mesh(new THREE.CircleGeometry(.8+Math.random()*2,20),new THREE.MeshBasicMaterial({color:0xff4b16,transparent:true,opacity:.5}));lava.rotation.x=-Math.PI/2;lava.position.set((Math.random()-.5)*18,.03,-8-Math.random()*65);world.add(lava)}tower(-8,-38,14,3);tower(8,-42,16,3.4);arch(0,-27,1.5)}
  else{for(let i=0;i<14;i++)mountain((Math.random()-.5)*60,-50-Math.random()*55,8+Math.random()*18,0x53626d);for(let i=0;i<20;i++){const trunk=new THREE.Mesh(new THREE.CylinderGeometry(.12,.18,4+Math.random()*3,6),new THREE.MeshStandardMaterial({color:0x1b2025,roughness:1}));trunk.position.set((Math.random()-.5)*20,2,-8-Math.random()*65);world.add(trunk)}}
  const rune=new THREE.Mesh(new THREE.RingGeometry(1.2,1.31,64),new THREE.MeshBasicMaterial({color:accent,transparent:true,opacity:.46,side:THREE.DoubleSide}));rune.rotation.x=-Math.PI/2;rune.position.set(0,.04,-4.2);world.add(rune);
  portal=new THREE.Mesh(new THREE.TorusGeometry(1.25,.14,20,72),new THREE.MeshStandardMaterial({color:0xbac4ff,emissive:accent,emissiveIntensity:2.2,roughness:.22,metalness:.72}));portal.position.set(0,1.85,-20);portal.rotation.x=Math.PI/2;portal.visible=false;world.add(portal);
  chest=new THREE.Group();const b=new THREE.Mesh(new THREE.BoxGeometry(1.3,.72,.86),new THREE.MeshStandardMaterial({color:0x6a4125,roughness:.7})),l=new THREE.Mesh(new THREE.BoxGeometry(1.3,.3,.86),new THREE.MeshStandardMaterial({color:0x9c7044,roughness:.55,metalness:.1}));l.position.y=.5;chest.add(b,l);chest.position.set(2.8,.4,-6.5);world.add(chest);
  blue.position.set(3.8,2,-9);fire.position.set(-3,2.4,-4);buildSwarm();
}
function makeWeapon(style){const g=new THREE.Group(),metal=new THREE.MeshStandardMaterial({color:0xc9d0d8,roughness:.2,metalness:.92,emissive:0x12090b}),dark=new THREE.MeshStandardMaterial({color:0x21150f,roughness:.9}),blood=new THREE.MeshStandardMaterial({color:0x6e0813,roughness:.58,metalness:.1});const blade=(x=0,long=1.4)=>{const main=new THREE.Mesh(new THREE.BoxGeometry(.095,long,.14),metal);main.position.set(x,long*.52,0);main.rotation.z=.04;const edge=new THREE.Mesh(new THREE.BoxGeometry(.105,long*.34,.148),blood);edge.position.set(x,long*.8,.002);const grip=new THREE.Mesh(new THREE.CylinderGeometry(.055,.072,.58,10),dark);grip.position.set(x,-.2,0);const guard=new THREE.Mesh(new THREE.BoxGeometry(.42,.07,.18),metal);guard.position.set(x,.1,0);g.add(main,edge,grip,guard)};if(style==='hammer'){const h=new THREE.Mesh(new THREE.CylinderGeometry(.055,.075,1.8,10),dark);h.rotation.z=-.62;h.position.set(.36,-.35,0);const head=new THREE.Mesh(new THREE.BoxGeometry(.78,.44,.42),metal);head.position.set(-.2,.22,0);g.add(h,head)}else if(style==='dual'){blade(-.18,1.12);blade(.35,1.02);g.children.slice(4).forEach(o=>o.position.z-=.17)}else if(style==='dagger'){blade(0,.9)}else blade(0,1.55);return g}
function buildFirstPerson(){while(camera.children.length){const c=camera.children[0];camera.remove(c);disposeObject(c)}const W=WEAPONS[minds[selected].g.weapon];weaponRig=new THREE.Group();weaponRig.position.set(.8,-.8,-1.62);weaponRig.rotation.z=-.46;weaponRig.add(makeWeapon(W.powerStyle==='hammer'?'hammer':W.tags.includes('dual')?'dual':W.tags.includes('dagger')?'dagger':'sword'));camera.add(weaponRig);const skin=new THREE.MeshStandardMaterial({color:0x755046,roughness:.95});for(const [x,y,z,r] of [[.48,-.72,-1.3,-.66],[.69,-.62,-1.38,-.96]]){const arm=new THREE.Mesh(new THREE.CapsuleGeometry(.075,.48,5,8),skin);arm.position.set(x,y,z);arm.rotation.z=r;camera.add(arm)}}
function buildSwarm(){clearGroup(swarmRoot);const count=+$('swarm').value;const geo=new THREE.CapsuleGeometry(.14,.55,3,5),mat=new THREE.MeshStandardMaterial({color:0x9d2a2c,roughness:.88,emissive:0x250205,emissiveIntensity:.3});swarmMesh=new THREE.InstancedMesh(geo,mat,count);const d=new THREE.Object3D();for(let i=0;i<count;i++){const z=-14-Math.random()*62,x=(Math.random()-.5)*(8+Math.abs(z)*.12);d.position.set(x,.55,z);d.scale.setScalar(.72+Math.random()*.55);d.rotation.y=Math.random()*Math.PI*2;d.updateMatrix();swarmMesh.setMatrixAt(i,d.matrix)}swarmMesh.instanceMatrix.needsUpdate=true;swarmRoot.add(swarmMesh)}
function proceduralEnemy(){const g=new THREE.Group(),mat=new THREE.MeshStandardMaterial({color:0xd8d2c8,roughness:.86});const body=new THREE.Mesh(new THREE.CapsuleGeometry(.38,1.08,6,12),mat);body.position.y=1.12;const head=new THREE.Mesh(new THREE.SphereGeometry(.27,16,16),mat);head.position.y=2.08;g.add(body,head);return {scene:g,animations:[]}}
function createHorn(){return new THREE.Mesh(new THREE.ConeGeometry(.08,.48,7),new THREE.MeshStandardMaterial({color:0xeee4d5,roughness:.74}))}
function activeAsset(){return customAsset||defaultAsset}
function activeAnimations(){return customAsset?customAnimations:defaultAnimations}
function createEnemy(i,count){const base=activeAsset(),source=base?{scene:SkeletonUtils.clone(base),animations:activeAnimations()}:proceduralEnemy(),root=source.scene;root.scale.setScalar(base?1.14:1);root.position.set(((i-(count-1)/2)/Math.max(1,count-1))*(3.1+i*.22),0,-5.5-i*2.5-Math.random());root.traverse(o=>{if(o.isMesh){o.castShadow=true;o.receiveShadow=true;const mats=(Array.isArray(o.material)?o.material:[o.material]).map(m=>{const n=m.clone();n.color?.setHex(0xd6d0c7);n.roughness=.82;n.metalness=.04;n.emissive=new THREE.Color(0x120306);n.emissiveIntensity=.2;return n});o.material=mats.length===1?mats[0]:mats}});const hl=createHorn(),hr=createHorn();hl.position.set(-.16,2.1,.03);hr.position.set(.16,2.1,.03);hl.rotation.z=.55;hr.rotation.z=-.55;root.add(hl,hr);const mixer=new THREE.AnimationMixer(root),actions={};for(const clip of source.animations)actions[clip.name]=mixer.clipAction(clip);const first=actions.Run||actions.Walk||actions.Idle||Object.values(actions)[0];first?.play();root.userData={mixer,actions,current:first,phase:Math.random()*Math.PI*2};enemyRoot.add(root);return root}
function switchAnim(e,name){const a=e.userData.actions,target=a?.[name]||a?.Run||a?.Walk||a?.Idle;if(!target||target===e.userData.current)return;e.userData.current?.fadeOut(.16);target.reset().fadeIn(.16).play();e.userData.current=target}
function syncEnemyCount(){const count=Math.min(9,minds[selected].foes);if(count===lastFoes)return;lastFoes=count;clearEnemies();enemyVisuals=[];for(let i=0;i<count;i++)enemyVisuals.push(createEnemy(i,count))}
function rebuildSelectedWorld(full=false){lastFoes=-1;if(full)buildWorld();buildFirstPerson();syncEnemyCount();hideEnd()}
function spawnBurst(type,count=18){const co