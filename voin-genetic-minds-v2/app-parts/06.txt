lors={blood:0xb31d31,ichor:0x69d7ff,gold:0xffbd5a,fire:0xff582d,ice:0x8de3ff,lightning:0x73cfff};const color=colors[type]||0xffffff;for(let i=0;i<count;i++){const p=new THREE.Mesh(new THREE.SphereGeometry(.022+Math.random()*.045,7,7),new THREE.MeshBasicMaterial({color,transparent:true}));p.position.set((Math.random()-.5)*1.7,.8+Math.random()*1.7,-3-Math.random()*2.4);p.userData={v:new THREE.Vector3((Math.random()-.5)*.085,.02+Math.random()*.065,(Math.random()-.5)*.055),life:.6+Math.random()*.65};fxRoot.add(p);particles.push(p)}}
function shockwave(color=0x70d8ff){const r=new THREE.Mesh(new THREE.RingGeometry(.3,.37,48),new THREE.MeshBasicMaterial({color,transparent:true,opacity:.9,side:THREE.DoubleSide}));r.rotation.x=-Math.PI/2;r.position.set(0,.06,-4);r.userData={life:.65};fxRoot.add(r);shockwaves.push(r)}
function makeLightning(){const pts=[];let p=new THREE.Vector3(.5,.1,-1.6),target=new THREE.Vector3((Math.random()-.5)*2,1.2,-4.2);pts.push(p.clone());for(let i=1;i<9;i++){const t=i/9,base=p.clone().lerp(target,t);base.x+=(Math.random()-.5)*.32;base.y+=(Math.random()-.5)*.22;pts.push(base)}pts.push(target);const line=new THREE.Line(new THREE.BufferGeometry().setFromPoints(pts),new THREE.LineBasicMaterial({color:0x77d9ff,transparent:true,opacity:1}));line.userData.life=.14;camera.add(line);lightningLines.push(line)}
function flashDamage(){$('damageFlash').classList.add('on');setTimeout(()=>$('damageFlash').classList.remove('on'),95)}
function showEnd(m){if(m.id!==selected)return;$('deathScreen').classList.remove('hidden');$('endKicker').textContent=m.state==='dead'?'GENOME FAILURE':'PORTAL EXTRACTION';$('endTitle').textContent=m.state==='dead'?'THE MIND DIED':'THE LINEAGE RETURNED';$('endBody').textContent=m.state==='dead'?`Trial ended with ${m.ichor|0} Ichor observed.`:`${m.ichor|0} Ichor · ${m.loot.toFixed(1)} loot · ${m.kills} kills.`}
function hideEnd(){$('deathScreen').classList.add('hidden')}
async function loadDefaultModel(){try{const gltf=await loader.loadAsync('https://threejs.org/examples/models/gltf/Soldier.glb');defaultAsset=gltf.scene;defaultAnimations=gltf.animations;$('modelStatus').textContent=`Default Soldier GLB · ${defaultAnimations.length} clips`;rebuildSelectedWorld(false)}catch(err){console.warn(err);$('modelStatus').textContent='Procedural enemy fallback'}}
async function loadModelFrom(url,label){try{$('modelStatus').textContent='Loading model…';const gltf=await loader.loadAsync(url);customAsset=gltf.scene;customAnimations=gltf.animations;$('modelStatus').textContent=`${label} · ${customAnimations.length} animation clips`;rebuildSelectedWorld(false)}catch(err){console.error(err);$('modelStatus').textContent='Model failed — check CORS and glTF validity'}}

function animateWorld(dt){const m=minds[selected];if(!m)return;const t=now()/1000;fire.intensity=5.5+Math.sin(t*8)*.8;blue.intensity=3.4+Math.sin(t*5.2+1)*.5;if(portal){portal.visible=m.action==='Extract'||m.state==='extracted';portal.rotation.z+=dt*2.5;portal.scale.setScalar(1+Math.sin(t*6)*.06)}if(chest){chest.visible=m.chest;chest.rotation.y=Math.sin(t*1.7)*.08}if(swarmMesh){swarmMesh.rotation.y=Math.sin(t*.18)*.025}
  const swing=m.swing?Math.sin((.36-m.swing)/.36*Math.PI):0;if(weaponRig){weaponRig.position.set(.8-swing*.5,-.8+swing*.25,-1.62+swing*.22);weaponRig.rotation.set(0,0,-.46+swing*1.42+m.hurt*.18)}camera.position.x=Math.sin(t*2.1)*.018+(Math.random()-.5)*m.hurt*.13;camera.position.y=1.66+Math.sin(t*4.2)*.018-m.hurt*.03;camera.rotation.z=(Math.random()-.5)*m.hurt*.08+m.dash*.05;camera.fov=72+m.dash*11+m.lightning*4;camera.updateProjectionMatrix();afterimage.enabled=m.dash>.05||m.lightning>.05;afterimage.uniforms.damp.value=m.dash>.05?.78:.86;
  syncEnemyCount();enemyVisuals.forEach((e,i)=>{e.userData.mixer?.update(dt);e.userData.phase+=dt*(1.25+i*.07);e.position.z+=dt*(.38+i*.03);if(e.position.z>-1.7)e.position.z=-2-i*.3;e.position.x+=Math.sin(e.userData.phase)*dt*.12;e.rotation.y=Math.sin(e.userData.phase*.72)*.1;switchAnim(e,e.position.z>-3.2?'Run':'Walk');if((m.action==='Attack'||m.action==='Heavy'||m.action==='Power')&&m.swing>.12&&i===0){e.rotation.z=Math.sin(t*24)*.13;e.traverse(o=>{if(o.isMesh){const mats=Array.isArray(o.material)?o.material:[o.material];mats.forEach(mat=>{if(mat?.emissive){mat.emissive.setHex(0x5b0611);mat.emissiveIntensity=1.3}})}})}else e.rotation.z=0});
  if(visualLightning>0){visualLightning-=dt;if(Math.random()<.45)makeLightning();if(Math.random()<.18)shockwave()}
  for(let i=particles.length-1;i>=0;i--){const p=particles[i];p.userData.life-=dt;p.position.add(p.userData.v);p.userData.v.y-=.003;p.material.opacity=clamp(p.userData.life);if(p.userData.life<=0){fxRoot.remove(p);disposeObject(p);particles.splice(i,1)}}
  for(let i=shockwaves.length-1;i>=0;i--){const r=shockwaves[i];r.userData.life-=dt;r.scale.multiplyScalar(1+dt*5);r.material.opacity=clamp(r.userData.life);if(r.userData.life<=0){fxRoot.remove(r);disposeObject(r);shockwaves.splice(i,1)}}
  for(let i=lightningLines.length-1;i>=0;i--){const l=lightningLines[i];l.userData.life-=dt;l.material.opacity=clamp(l.userData.life/.14);if(l.userData.life<=0){camera.remove(l);disposeObject(l);lightningLines.splice(i,1)}}
  const cd=1-clamp(m.cooldown);$('ability1').querySelector('i').style.transform=`scaleX(${cd})`;$('ability2').querySelector('i').style.transform=`scaleX(${clamp(m.runeHits/(RUNES[m.g.rune].hits||4))})`;$('ability3').querySelector('i').style.transform=`scaleX(${1-clamp(m.dash/.45)})`;$('ability4').querySelector('i').style.transform=`scaleX(${m.powerCharge})`;
}
function renderLoop(){resize();const dt=Math.min(.04,clock.getDelta());animateWorld(dt);composer.render();requestAnimationFrame(renderLoop)}

// ----- Optimizer worker -----
function ensureWorker(){if(optimizerWorker)return optimizerWorker;optimizerWorker=new Worker(new URL('./optimizer-worker.js?v=3',location.href),{type:'module'});optimizerWorker.onmessage=e=>{const d=e.data;if(d.type==='progress'){const pct=d.generation/d.total*100;$('optimizerProgress').querySelector('i').style.width=`${pct}%`;$('optimizerStatus').textContent=`Generation ${d.generation}/${d.total} · ${d.archive} niches`;$('archive').textContent=d.archive}else if(d.type==='done'){optimizerRunning=false;$('optimizerProgress').querySelector('i').style.width='100%';$('optimizerStatus').textContent=`${d.evaluations.toLocaleString()} noisy episodes · ${d.pareto.length} Pareto builds`;$('evaluations').textContent=(totalEvaluations+d.evaluations).toLocaleString();renderOptimizerResults(d.top)}};optimizerWorker.onerror=e=>{optimizerRunning=false;$('optimizerStatus').textContent='Worker failed';console.error(e)};return optimizerWorker}
function runOptimizer(){if(optimizerRunning)return;optimizerRunning=true;$('optimizerResults').innerHTML='<div class="empty">Running noisy multi-objective evolution…</div>';$('optimizerProgress').querySelector('i').style.width='2%';$('optimizerStatus').textContent='Initializing community priors';ensureWorker().postMessage({type:'search',g