import fs from 'node:fs';import crypto from 'node:crypto';import assert from 'node:assert/strict';
const sha=b=>crypto.createHash('sha256').update(b).digest('hex');
async function instantiateBytes(bytes,label){try{return (await WebAssembly.instantiate(bytes,{})).instance.exports;}catch(err){console.error(`${label}_INSTANTIATE_FAIL: ${err?.message||err}`);throw err;}}
function fixture(e){assert.equal(e.genesis_model_version()>>>0,0x00040001);for(const name of ['genesis_fluid_divergence_before','genesis_fluid_divergence_after','genesis_fluid_kinetic_energy','genesis_fluid_momentum_x','genesis_fluid_momentum_y','genesis_fluid_momentum_z','genesis_impulse_water'])assert.equal(typeof e[name],'function',`missing ${name}`);e.genesis_init(32,24,32,123456,72);e.genesis_seed_scene();const s0=e.genesis_sand_mass_units()>>>0,w0=e.genesis_water_cells()>>>0,h0=e.genesis_hash()>>>0;assert.ok((e.genesis_impulse_water(24,7,10,6,420,0,160)|0)>0);assert.ok((e.genesis_fluid_momentum_x()|0)>0);e.genesis_step(1);const divBefore=e.genesis_fluid_divergence_before()>>>0,divAfter=e.genesis_fluid_divergence_after()>>>0;assert.ok(divBefore>0);assert.ok(divAfter<=divBefore,`projection ${divBefore}->${divAfter}`);assert.ok((e.genesis_fluid_kinetic_energy()>>>0)>0);for(let i=1;i<80;i++)e.genesis_step(1);const out={hash:e.genesis_hash()>>>0,sand:e.genesis_sand_mass_units()>>>0,water:e.genesis_water_cells()>>>0,erosion:e.genesis_erosion_events()>>>0,deposition:e.genesis_deposition_events()>>>0,divBefore,divAfter,kinetic:e.genesis_fluid_kinetic_energy()>>>0,momentum:[e.genesis_fluid_momentum_x()|0,e.genesis_fluid_momentum_y()|0,e.genesis_fluid_momentum_z()|0],invariant:e.genesis_invariant()};assert.equal(out.sand,s0);assert.equal(out.water,w0);assert.notEqual(out.hash,h0);assert.equal(out.invariant,1);assert.ok(out.erosion>0);assert.ok(out.deposition>0);assert.ok(out.kinetic>0);e.genesis_init(32,24,32,123456,72);e.genesis_seed_scene();e.genesis_impulse_water(24,7,10,6,420,0,160);for(let i=0;i<80;i++)e.genesis_step(1);assert.equal(e.genesis_hash()>>>0,out.hash);return out;}
const manifest=JSON.parse(fs.readFileSync(new URL('./solver.v3.manifest.json',import.meta.url),'utf8'));let joined='';for(const p of manifest.parts){const t=fs.readFileSync(new URL('./'+p.file,import.meta.url),'utf8').trim();assert.equal(t.length,p.chars,p.file+' chars');assert.equal(sha(t),p.sha256,p.file+' text SHA');joined+=t;}assert.equal(joined.length,manifest.base64_chars);assert.equal(sha(joined),manifest.base64_sha256);const committed=Buffer.from(joined,'base64');assert.equal(committed.byteLength,manifest.decoded_bytes);assert.equal(sha(committed),manifest.decoded_sha256);let compiledResult=null;const compiledPath=process.argv[2]||null;if(compiledPath){const compiled=fs.readFileSync(compiledPath);const compiledHash=sha(compiled);console.log(JSON.stringify({freshCompiledBytes:compiled.byteLength,freshCompiledSha256:compiledHash}));assert.equal(compiled.byteLength,manifest.decoded_bytes);assert.equal(compiledHash,manifest.decoded_sha256,'fresh compiler output must equal promoted browser payload hash');compiledResult=fixture(await instantiateBytes(compiled,'FRESH_COMPILED_WASM'));console.log('PASS fresh C++ -> WebAssembly projection fixture');}console.log(JSON.stringify({committedBrowserParts:manifest.parts.length,committedBrowserBytes:committed.byteLength,committedBrowserSha256:sha(committed)}));const committedResult=fixture(await instantiateBytes(committed,'COMMITTED_BROWSER_WASM'));if(compiledResult)assert.deepEqual(compiledResult,committedResult);console.log(JSON.stringify({model:'genesis-volume3d-projection-v1',committedResult,compiledResult}));console.log('PASS GENESIS exact chunked browser WASM matches fresh C++ projection build and semantics');
