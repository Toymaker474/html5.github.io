name: Hourly Teacher Health
on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  teacher-health:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate teacher engines
        run: |
          node --check sandbox-god-genic/src/teacher.js
          python -m py_compile sandbox-god-genic/python/teacher.py
      - name: Check public Scientific Genesis v8 deployment
        run: |
          base="https://toymaker474.github.io/html5.github.io"
          curl --fail --retry 3 --retry-delay 8 --max-time 30 "$base/" \
            | grep -q "OPENING SCIENTIFIC GENESIS"
          curl --fail --retry 3 --retry-delay 8 --max-time 30 "$base/god-sandbox/" \
            | grep -q "Build the laws first"
          for asset in style.css app.js sim.js; do
            curl --fail --retry 3 --retry-delay 8 --max-time 30 \
              "$base/god-sandbox/$asset?v=8" >/dev/null
          done
          curl --fail --retry 3 --retry-delay 8 --max-time 30 \
            "$base/sw.js?v=8" >/dev/null
