const CACHE='sandbox-god-genic-v1';
const CORE=['../','../index.html','./styles.css?v=1','./src/main.js?v=1','./src/world.js','./src/brain.js','./src/teacher.js','./src/renderer.js','../manifest.webmanifest?v=1','../icon.svg?v=1'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>Promise.allSettled(CORE.map(url=>cache.add(new Request(url,{cache:'reload'}))))).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('sandbox-god-genic-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const u=new URL(event.request.url);if(u.origin!==location.origin)return;const nav=event.request.mode==='navigate';event.respondWith(nav?fetch(event.request).then(r=>{const c=r.clone();caches.open(CACHE).then(x=>x.put(event.request,c));return r}).catch(()=>caches.match('../index.html')):caches.match(event.request).then(hit=>hit||fetch(event.request).then(r=>{const c=r.clone();caches.open(CACHE).then(x=>x.put(event.request,c));return r})))});
