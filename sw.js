const CACHE='sandbox-god-genic-root-v1';
const CORE=['./','./index.html','./manifest.webmanifest?v=1','./icon.svg?v=1','./sandbox-god-genic/styles.css?v=1','./sandbox-god-genic/src/main.js?v=1','./sandbox-god-genic/src/world.js','./sandbox-god-genic/src/brain.js','./sandbox-god-genic/src/teacher.js','./sandbox-god-genic/src/renderer.js'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>Promise.allSettled(CORE.map(url=>cache.add(new Request(url,{cache:'reload'}))))).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(url.origin!==location.origin)return;const nav=event.request.mode==='navigate';event.respondWith(nav?fetch(event.request).then(response=>{if(response.ok)caches.open(CACHE).then(cache=>cache.put(event.request,response.clone()));return response}).catch(()=>caches.match('./index.html')):caches.match(event.request).then(cached=>cached||fetch(event.request).then(response=>{if(response.ok)caches.open(CACHE).then(cache=>cache.put(event.request,response.clone()));return response})))});
